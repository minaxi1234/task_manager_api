# Task Manager API ✅

A simple **FastAPI-based Task Manager** application with authentication, CRUD operations, filtering, and sorting.

## Features
- User registration & login with JWT authentication
- Create, read, update, and delete tasks
- Filtering & sorting of tasks
- Pagination support
- Role-based access (admin and normal user)

## Tech Stack
- **FastAPI** - Web framework
- **SQLite / SQLAlchemy** - Database
- **JWT (JSON Web Tokens)** - Authentication
- **Uvicorn** - ASGI server

## Installation (Local)
1. Clone the repository:
   ```bash
   git clone https://github.com/minaxi1234/task_manager_api.git
   cd task_manager_api
